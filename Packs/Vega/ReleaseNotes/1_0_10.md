
#### Classifiers

##### Vega Mapper

- Wrap **Vega Detection Query** in markdown SQL code fences so the query renders as a code block in the alert layout.

#### Incident Fields

##### Vega Detection Query

- Changed field type from `longText` to `markdown` to support code-block rendering.
