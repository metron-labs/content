
#### Integrations

##### Vega

- Normalize Vega platform links by replacing the `api` host subdomain with `app` before XSOAR ingestion.
- Build Vega alert links from the configured integration URL (with `api` replaced by `app`) when the API does not return a link.

#### Classifiers

##### Vega Mapper

- Map alert `link` to Vega Alert Link and External Link fields.

#### Incident Fields

##### New: Vega Alert Link

- Added a URL field for direct navigation to alerts in the Vega platform.
