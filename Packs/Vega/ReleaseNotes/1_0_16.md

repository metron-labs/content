
#### Integrations

##### Vega

- Fixed an issue where changing alert or incident fetch filters (severity, status, or verdict) did not re-fetch matching entities from the configured backfill window.
- Fetch cursors now reset independently for alerts and incidents when their respective filter configuration changes, while still deduplicating already-ingested entities.
